export function CreateTodoButton(props) {
    const {setTodos} = props 
    return (<button 
        onClick={console.log}
        style={{
        display: "flex",
        margin: "auto"
    }}>+</button>)
}